{
  "name": "chakuyomi-functions",
  "description": "チャクヨミ - 認証・課金・予測配信",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "test": "node auth.test.js && node billing.test.js && node api.test.js",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "stripe": "^14.0.0"
  },
  "private": true
}
